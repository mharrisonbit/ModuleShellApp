<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns="http://xamarin.com/schemas/2014/forms"
             x:Class="PaymentsModule.Views.PaymentsView"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Label Text="{Binding Title}"
               Grid.Row="0"/>
        <Entry Placeholder="Amount to pay"
               Grid.Row="1"
               x:Name="PayAmountEntry"/>
        <Button Text="Pay Now"
                Grid.Row="2"
                Command="{Binding PayBtnClk}"
                CommandParameter="{Binding Source={x:Reference PayAmountEntry}, Path=Text}"/>

     </Grid>
</ContentPage>
